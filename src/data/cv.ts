/** Certificações que não devem aparecer no CV (ex.: licenciaturas) */
export const CV_EXCLUDED_CERTIFICATION_IDS = ["ise-c-computer-engineering"] as const;

/** IDs de projetos académicos incluídos no CV */
export const CV_ACADEMIC_PROJECT_IDS = ["java"] as const;

export const CV_MAIN_PROJECT_ID = "flowjersey" as const;
